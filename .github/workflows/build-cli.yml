name: Build CLI

on:
  workflow_call:
    inputs:
      release-tag:
        required: true
        type: string

jobs:
  docker:
    name: Docker
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set release version
        run: |
          sed -i 's/version = "[0-9.]*"$/version = "${{ inputs.release-tag }}"/' Cargo.toml
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      - name: Build image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: runtime/cli/build.Dockerfile
          # NOTE: ARM64 support temporarily disabled due to slow QEMU emulation on standard runners.
          platforms: linux/amd64
          tags: |
            ghcr.io/eddmann/santa-lang-cli:latest
            ghcr.io/eddmann/santa-lang-comet:cli-latest
          labels: org.opencontainers.image.source=https://github.com/eddmann/santa-lang-comet
          outputs: type=docker,dest=/tmp/santa-lang-comet-cli.tar
      - name: Upload
        run: |
          mv /tmp/santa-lang-comet-cli.tar santa-lang-comet-cli-${{ inputs.release-tag }}-docker.tar
          gh release upload ${{ inputs.release-tag }} santa-lang-comet-cli-${{ inputs.release-tag }}-docker.tar --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  binary:
    name: Binary
    runs-on: ${{ matrix.os }}
    permissions:
      contents: write
    env:
      CARGO_TERM_COLOR: always
    strategy:
      matrix:
        include:
          - platform: linux-amd64
            target: x86_64-unknown-linux-gnu
            os: ubuntu-24.04
          - platform: linux-arm64
            target: aarch64-unknown-linux-gnu
            os: ubuntu-24.04
          - platform: macos-amd64
            target: x86_64-apple-darwin
            os: macos-14
          - platform: macos-arm64
            target: aarch64-apple-darwin
            os: macos-14
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Set release version
        run: |
          sed -i.bak 's/version = "[0-9.]*"$/version = "${{ inputs.release-tag }}"/' Cargo.toml
      - name: Install cross-compilation tools (ARM64 Linux)
        if: matrix.platform == 'linux-arm64'
        run: |
          sudo apt-get update
          sudo apt-get install -y gcc-aarch64-linux-gnu g++-aarch64-linux-gnu
      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          toolchain: 1.85.0
          targets: ${{ matrix.target }}
      - name: Load cache
        uses: Swatinem/rust-cache@v2
      - name: Configure cross-compilation (ARM64 Linux)
        if: matrix.platform == 'linux-arm64'
        run: |
          echo "[target.aarch64-unknown-linux-gnu]" >> ~/.cargo/config.toml
          echo "linker = \"aarch64-linux-gnu-gcc\"" >> ~/.cargo/config.toml
      - name: Build
        run: cargo build --release --bin santa-cli --target ${{ matrix.target }}
      - name: Upload
        run: |
          cp target/${{ matrix.target }}/release/santa-cli santa-lang-comet-cli-${{ inputs.release-tag }}-${{ matrix.platform }}
          gh release upload ${{ inputs.release-tag }} santa-lang-comet-cli-${{ inputs.release-tag }}-${{ matrix.platform }} --clobber
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
