name: Test

on:
  push:

jobs:
  test:
    name: Test
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Load Cache
        uses: Swatinem/rust-cache@v2
      - name: Test
        run: make can-release
